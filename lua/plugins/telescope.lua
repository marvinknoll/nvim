return {
  "nvim-telescope/telescope.nvim",
  defaults = {
    vimgrep_arguments = {
      -- 'rg',
      -- '--color=never',
      -- '--no-heading',
      -- '--with-filename',
      -- '--line-number',
      -- '--column',
      -- '--smart-case',
      "--no-ignore-vcs",
      "--hidden",
    },
  },
}
